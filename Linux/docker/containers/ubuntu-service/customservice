service sample_service
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = serviceuser
    bind        = 0.0.0.0
    server      = /home/serviceuser/program
    type        = UNLISTED
    port        = 8000
}

